INSERT INTO setting_template (
    id,
    level,
    service_id,
    version,
    inbound_rate_limit,
    inbound_rate_limit_window_seconds,
    inbound_timeout_ms,
    inbound_request_size_limit_kb,
    inbound_response_size_limit_kb,
    inbound_response_time_threshold_ms,
    inbound_log_retention_days,
    outbound_timeout_ms,
    outbound_retry_count,
    outbound_retry_backoff_ms,
    outbound_response_time_threshold_ms,
    outbound_log_retention_days,
    outbound_rollback_strategy,
    alert_severity,
    alert_throttle_minutes,
    alert_channels,
    created_at,
    updated_at
)
SELECT
    'global-template-seed',
    'GLOBAL',
    NULL,
    0,
    100,
    60,
    30000,
    10240,
    10240,
    5000,
    30,
    30000,
    3,
    1000,
    5000,
    30,
    'IGNORE',
    'WARNING',
    5,
    '["EMAIL"]',
    NOW(),
    NOW()
WHERE NOT EXISTS (
    SELECT 1 FROM setting_template WHERE level = 'GLOBAL'
);
